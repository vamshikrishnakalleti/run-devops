apiVersion: v1
kind: Service
metadata:
    name: vamshikrishnakalletirundevops
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: vamshikrishnakalletirundevops